<?xml version="1.0" encoding="UTF-8"?>
<project name="kindlet-api" default="jar" basedir=".">
    <property name="src.dir" value="src/main/java"/>
    <property name="test.dir" value="src/test/java"/>
    <property name="build.dir" value="build"/>
    <property name="classes.dir" value="${build.dir}/classes"/>
    <property name="test.classes.dir" value="${build.dir}/test-classes"/>
    <property name="dist.dir" value="dist"/>
    <property name="jar.name" value="kindlet-api.jar"/>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="init">
        <mkdir dir="${classes.dir}"/>
        <mkdir dir="${test.classes.dir}"/>
        <mkdir dir="${dist.dir}"/>
    </target>

    <target name="compile" depends="init">
        <javac srcdir="${src.dir}" destdir="${classes.dir}"
               source="1.4" target="1.4"
               includeantruntime="false" debug="true"/>
    </target>

    <target name="jar" depends="compile">
        <jar destfile="${dist.dir}/${jar.name}" basedir="${classes.dir}">
            <manifest>
                <attribute name="Implementation-Title" value="Amazon Kindlet API Stubs"/>
                <attribute name="Implementation-Version" value="2.1"/>
                <attribute name="Specification-Title" value="Personal Basis Profile"/>
                <attribute name="Specification-Version" value="1.1"/>
            </manifest>
        </jar>
    </target>

    <target name="compile-test" depends="compile">
        <javac srcdir="${test.dir}" destdir="${test.classes.dir}"
               classpath="${classes.dir}"
               source="1.4" target="1.4"
               includeantruntime="false" debug="true"/>
    </target>

    <target name="test" depends="compile-test">
        <java classname="com.amazon.kindle.kindlet.KindletLifecycleTest" failonerror="true">
            <classpath>
                <pathelement path="${classes.dir}"/>
                <pathelement path="${test.classes.dir}"/>
            </classpath>
        </java>
    </target>
</project>
